/**
 * Global UI behaviour settings.
 * Adjust these values here to affect every component that uses them.
 */

/** How long (ms) the user must hold a widget before the hold-to-reset action fires. */
export const HOLD_TO_RESET_MS = 500;
